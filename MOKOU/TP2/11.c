/* UNRN PyA - Jose Emanuel Suez

11) Especificar, para las siguientes lineas de código qué significa cada una:

a) int (*puntero_funcion)(int);
b) int *puntero_funcion (int);

Si el objetivo es declarar un puntero a función que retorna un int y recibe un int. ¿Son ambas declaraciones
correctas?*/

/*
a) es un puntero a funcion del tipo int que recive como argumento un int.
b) esta escrita con sintaxis invalida (le falta los parentesis), por lo que para el compilador no es nada y producira un error cuando intente asignarsele alguna funcion.
Por lo tanto, solo la declaracion a) es la unica correcta.
*/