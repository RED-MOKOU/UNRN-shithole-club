/*This program did something I forgot already what it was but it works, i think*/
#include <stdio.h>

int main(int argc, char const *argv[])
{
	char car = 'a'; //ASCII code 97
	for(;;) {
		if (char == 'z') //ASCII code 122
			break;
		printf("%c", car);
		car++;
	}
	return 0;
}
